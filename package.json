{
  "name": "masqagrid",
  "version": "0.1.0",
  "private": true,
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "next dev",
    "dev:electron": "concurrently \"cross-env NODE_ENV=development next dev\" \"wait-on http://localhost:3000 && cross-env NODE_ENV=development electron .\"",
    "build": "npm run build:electron",
    "build:web": "next build",
    "build:nextjs": "cross-env NODE_ENV=production next build",
    "build:electron-main": "tsc -p electron/tsconfig.json",
    "build:electron": "npm run build:nextjs && npm run build:electron-main && electron-builder",
    "build:mac": "npm run build:nextjs && npm run build:electron-main && electron-builder --mac",
    "build:win": "npm run build:nextjs && npm run build:electron-main && electron-builder --win",
    "build:linux": "npm run build:nextjs && npm run build:electron-main && electron-builder --linux",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "electron-serve": "^3.0.1",
    "lucide-react": "^0.563.0",
    "next": "16.1.6",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "electron": "^40.1.0",
    "electron-builder": "^26.7.0",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "tailwindcss": "^4",
    "typescript": "^5",
    "wait-on": "^9.0.3"
  }
}
